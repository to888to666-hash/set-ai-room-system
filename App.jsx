import React, { useState } from 'react';
import { motion } from 'framer-motion';
import { Cpu, Eye, EyeOff, History, KeyRound, Lock, Plus, Power, Search, Sparkles } from 'lucide-react';

const MASTER_CODE = 'coco8899';
const DEFAULT_FORM = { roomNo:'', bankroll:'', unopened:'', prev1:'', prev2:'', todayBet:'', todayRate:'', monthBet:'', monthRate:'' };
const PANEL = 'relative overflow-hidden rounded-[2rem] border border-cyan-300/30 bg-slate-950/75 shadow-[0_0_24px_rgba(34,211,238,.18),0_0_60px_rgba(14,165,233,.14),inset_0_0_24px_rgba(34,211,238,.08)] backdrop-blur-xl ring-1 ring-cyan-200/10';

function Button({ children, className='', ...props }) { return <button {...props} className={`inline-flex items-center justify-center font-bold transition ${className}`}>{children}</button>; }
function Card({ children, className='' }) { return <section className={className}>{children}</section>; }
function CardContent({ children, className='' }) { return <div className={className}>{children}</div>; }
function nowTime(){ return new Date().toLocaleString('zh-TW',{hour12:false}); }
function toNumber(v){ const n=Number(v); return Number.isFinite(n)?n:0; }
function clamp(v,min,max){ return Math.max(min, Math.min(max, v)); }
function round(v,d=1){ return Number(v.toFixed(d)); }
function safeId(){ return crypto?.randomUUID?.() || `id-${Date.now()}-${Math.random().toString(16).slice(2)}`; }
function makeTempCode(){ const c='ABCDEFGHJKLMNPQRSTUVWXYZ23456789'; let out='SET-'; for(let i=0;i<6;i++) out+=c[Math.floor(Math.random()*c.length)]; return out; }

function analyzeMachine(form, isMaster=false){
  const n=Object.fromEntries(Object.entries(form).map(([k,v])=>[k,toNumber(v)]));
  const bankroll=Math.max(n.bankroll,1), unopened=Math.max(Math.round(n.unopened),0), prev1=Math.max(Math.round(n.prev1),0), prev2=Math.max(Math.round(n.prev2),0);
  const monthBet=Math.max(n.monthBet,1), todayBet=Math.max(n.todayBet,0), todayRate=n.todayRate, monthRate=n.monthRate;
  const pushRate=(todayBet/monthBet)*100, rateGap=todayRate-monthRate;
  const longTermBiteBias=clamp(100-monthRate,0,45), longTermLooseBias=clamp(monthRate-100,0,45);
  const unopenedCurve=clamp((1-Math.exp(-Math.pow(unopened/95,1.28)))*100,0,100);
  const prev1Curve=clamp((1-Math.exp(-Math.pow(prev1/92,1.2)))*100,0,100);
  const prev2Curve=clamp((1-Math.exp(-Math.pow(prev2/92,1.12)))*100,0,100);
  const recentOpenMemory=clamp(prev1Curve*.62+prev2Curve*.38,0,100);
  const longPreviousAverage=(prev1+prev2)/2;
  const earlyTriggerContrast=clamp((longPreviousAverage-unopened)*.42+Math.max(0,recentOpenMemory-52)*.35-Math.max(0,unopened-75)*.18,0,100);
  const highTurnPressure=clamp(unopenedCurve*.78+recentOpenMemory*.22,0,100);
  const cycleDeviation=clamp(Math.abs(unopened-longPreviousAverage)*.58,0,100);
  const rhythmConsistency=clamp(100-Math.abs(prev1-prev2)*1.25,0,100);
  let phase='低量初判段', phaseWeight=.72;
  if(pushRate>=.35&&pushRate<2.2){phase='資金初段';phaseWeight=.88;} else if(pushRate>=2.2&&pushRate<6.8){phase='資金累積段';phaseWeight=1.12;} else if(pushRate>=6.8&&pushRate<11.5){phase='資金活躍段';phaseWeight=1.2;} else if(pushRate>=11.5){phase='資金過熱段';phaseWeight=1.32;}
  const currentBite=Math.max(0,monthRate-todayRate), currentPayout=Math.max(0,todayRate-monthRate);
  const priorOpportunity=clamp(longTermBiteBias*.9+highTurnPressure*.34+earlyTriggerContrast*.42+rhythmConsistency*.08,0,100);
  const likelihoodAdjustment=clamp(currentBite*1.42+pushRate*.95+cycleDeviation*.24+Math.max(0,100-todayRate)*.34-currentPayout*1.15,-45,80);
  const posteriorOpportunity=clamp(priorOpportunity*.62+likelihoodAdjustment*.38,0,100);
  const chaseRisk=clamp(currentPayout*2.35+pushRate*3.15+Math.max(0,todayRate-108)*3.1+longTermLooseBias*1.4-earlyTriggerContrast*.12,0,100);
  const pullbackRisk=clamp(currentPayout*1.72+pushRate*2.35+chaseRisk*.24,0,100);
  const accumulationPressure=clamp((currentBite*2.25+longTermBiteBias*1.18+highTurnPressure*.42+recentOpenMemory*.26+earlyTriggerContrast*.38+pushRate*1.15)*phaseWeight,0,100);
  const volatilityPressure=clamp(Math.abs(rateGap)*1.08+pushRate*2.25+cycleDeviation*.22+recentOpenMemory*.12+chaseRisk*.28,0,100);
  const biteTrend=clamp(currentBite*2.22+longTermBiteBias*.82+pushRate*1.08+highTurnPressure*.28+recentOpenMemory*.18,0,100);
  const recoveryPotential=clamp(posteriorOpportunity*.48+accumulationPressure*.28+earlyTriggerContrast*.24+rhythmConsistency*.06-chaseRisk*.46-pullbackRisk*.18,0,100);
  const reboundTrend=clamp(recoveryPotential*.62+currentPayout*.56+earlyTriggerContrast*.12-pullbackRisk*.32,0,100);
  const bankrollPressureRatio=bankroll/Math.max(volatilityPressure*64,1);
  let bankrollFit='B', bankrollAdvice='目前可觀察，但建議控制節奏與停損';
  if(bankrollPressureRatio>=1.45){bankrollFit='S';bankrollAdvice='你的本金對目前波動來說相對舒服，可以正常節奏觀察';}
  else if(bankrollPressureRatio>=1.02){bankrollFit='A';bankrollAdvice='本金仍可承受目前波動，但建議分段測試，不要一次打深';}
  else if(bankrollPressureRatio>=.68){bankrollFit='B';bankrollAdvice='本金能碰，但容錯不高，適合小額測試、快進快出';}
  else {bankrollFit='C';bankrollAdvice='本金承受力偏低，這台目前不適合硬扛波動';}
  const bankrollBonus=bankrollFit==='S'?18:bankrollFit==='A'?11:bankrollFit==='B'?2:-16;
  const phaseBonus=phase==='資金累積段'?12:phase==='資金活躍段'?8:phase==='資金初段'?3:phase==='資金過熱段'?-18:-2;
  const operationScore=clamp(posteriorOpportunity*.28+recoveryPotential*.34+accumulationPressure*.18+earlyTriggerContrast*.16+bankrollBonus+phaseBonus-chaseRisk*.34-volatilityPressure*.14,0,100);
  const dataCompleteness=clamp((unopened>0?18:0)+(prev1>0?14:0)+(prev2>0?12:0)+(todayBet>0?18:0)+(monthBet>1?18:0)+(todayRate>0?10:0)+(monthRate>0?10:0),0,100);
  const confidence=clamp(dataCompleteness*.42+pushRate*5.8+Math.min(Math.abs(rateGap)*1.4,20)+Math.min(recentOpenMemory*.16,16),10,100);
  let label='平衡盤', advice='目前所有數據綜合後沒有形成明顯優勢，只能視為普通觀察盤', actionLevel='觀察', actionText='先不要急著操作，等下一段數據更明顯再判斷';
  if(chaseRisk>=72&&currentPayout>=8){label='追高風險區';advice='得分率已明顯高於長期基準，現在看到的回分可能已經是前段玩家吃掉的結果';actionLevel='不要追';actionText='這台現在最怕追高，建議跳過或等下一輪數據回落後再看';}
  else if(phase==='資金過熱段'&&currentPayout>=6){label='回吐後過熱區';advice='今日資金已大量推進，得分率也已拉高，可能已經回吐過一段';actionLevel='不要追';actionText='不建議再追這台，除非後續數據重新沉澱，否則容易追在高點';}
  else if(phase==='資金過熱段'){label='資金高壓區';advice='今日投注佔近30日資金池比例偏高，波動壓力較大，需要非常保守';actionLevel=bankrollFit==='S'||bankrollFit==='A'?'高風險短測':'不建議';actionText=bankrollFit==='S'||bankrollFit==='A'?'只適合小段測試，不適合連續追打':'不建議操作，小本金很容易被波動洗出去';}
  else if(earlyTriggerContrast>=42&&posteriorOpportunity>=45&&chaseRisk<55){label='低轉反差機會區';advice='前一前二偏長開，但目前未開轉數相對較低，形成低轉反差訊號；這只是概率加權，不代表必進';actionLevel=bankrollFit==='C'?'觀察':'可小測';actionText=bankrollFit==='C'?'有反差機會，但本金容錯不高，建議先看不要硬碰':'可用小節奏測試低轉反差，但沒反應就退，不要硬磨';}
  else if(currentBite>=6&&accumulationPressure>=38&&posteriorOpportunity>=38){label='咬分累積區';advice='得分率低於近30日基準，且未開/前一/前二與資金曲線共同形成累積壓力，但不代表下一段一定回';actionLevel=operationScore>=62?'可小測':bankrollFit==='C'?'不建議':'謹慎';actionText=operationScore>=62?'可以小額測試，但不要重壓，設定停損，沒進入節奏就離開':bankrollFit==='C'?'這台有累積感，但你的本金承受力不足，建議不要硬進':'可以觀察，不建議急著打深，等曲線更明顯再進';}
  else if(currentPayout>=6){label='回分活躍區';advice='目前得分率高於長期基準，代表今天已有回吐跡象，但不代表後面會繼續回';actionLevel=chaseRisk>=55?'謹慎':'可短打';actionText=chaseRisk>=55?'可以看，但不要追太深，這種盤容易從回分轉成回落':'若要操作只適合短打，吃到節奏就收，不適合長時間硬磨';}
  else if(operationScore>=58&&bankrollFit!=='C'){label='可觀察測試區';advice='全部數據融合後，機會分數與本金適配度尚可，但仍不是保證盤';actionLevel='可小測';actionText='可以小額測試節奏，但不要把它當成必回盤';}
  else if(pushRate<.35){label='低量初判區';advice='當日投注量偏低，但系統已納入未開、前一、前二與長期基準做初步推估';actionLevel=posteriorOpportunity>=45&&bankrollFit!=='C'?'可觀察小測':'觀察';actionText=posteriorOpportunity>=45&&bankrollFit!=='C'?'不是不能看，但只能小測，不能把低量初判當成成形盤':'目前訊號還不夠立體，先觀察，不建議急著操作';}
  const aiIndex=reboundTrend-biteTrend;
  return { id:safeId(), time:nowTime(), role:isMaster?'最高權限':'臨時權限', input:{...form, unopened, prev1, prev2}, roomNo:form.roomNo||'未填房號', biteScore:round(biteTrend), returnScore:round(reboundTrend), aiIndex:round(aiIndex), riskIndex:round(volatilityPressure), confidence:round(confidence), volumeRatio:round(pushRate,2), rateGap:round(rateGap,2), chaseRisk:round(chaseRisk), pullbackRisk:round(pullbackRisk), recoveryPotential:round(recoveryPotential), accumulationPressure:round(accumulationPressure), posteriorOpportunity:round(posteriorOpportunity), priorOpportunity:round(priorOpportunity), earlyTriggerContrast:round(earlyTriggerContrast), highTurnPressure:round(highTurnPressure), recentOpenMemory:round(recentOpenMemory), cycleDeviation:round(cycleDeviation), rhythmConsistency:round(rhythmConsistency), operationScore:round(operationScore), phase, bankrollFit, bankrollAdvice, actionLevel, actionText, level:bankrollFit, net:round(aiIndex), label, advice };
}

function Field({label,value,setValue,placeholder,integerOnly=false}){return <label className="block"><div className="mb-2 text-xs tracking-widest text-cyan-200/80">{label}</div><input type={label==='房號'?'text':'number'} step={integerOnly?'1':'0.01'} value={value} onChange={e=>setValue(integerOnly?e.target.value.replace(/[^0-9]/g,''):e.target.value)} placeholder={placeholder||'0'} className="w-full rounded-2xl border border-cyan-300/20 bg-slate-950/70 px-4 py-3 text-white outline-none transition focus:border-cyan-300/70 focus:ring-2 focus:ring-cyan-400/20" /></label>}
function MetricCard({title,value,tone='cyan'}){const m={cyan:'border-cyan-300/20 bg-cyan-500/10 text-cyan-100',emerald:'border-emerald-300/20 bg-emerald-500/10 text-emerald-100',rose:'border-rose-300/20 bg-rose-500/10 text-rose-100',yellow:'border-yellow-300/20 bg-yellow-500/10 text-yellow-100'};return <div className={`rounded-3xl border p-5 ${m[tone]}`}><div className="text-sm opacity-75">{title}</div><div className="mt-2 text-3xl font-black">{value}</div></div>}
function Title({children}){return <h2 className="relative inline-block text-2xl font-black tracking-[0.18em] text-cyan-100 drop-shadow-[0_0_18px_rgba(34,211,238,0.95)]">{children}</h2>}
function CircuitBackground(){return <div className="pointer-events-none fixed inset-0 overflow-hidden"><div className="absolute inset-0 bg-[radial-gradient(circle_at_20%_20%,rgba(34,211,238,0.18),transparent_22%),radial-gradient(circle_at_80%_30%,rgba(59,130,246,0.18),transparent_24%),radial-gradient(circle_at_50%_80%,rgba(6,182,212,0.12),transparent_28%)]"/><div className="absolute inset-0 opacity-40 [background-image:linear-gradient(rgba(34,211,238,0.08)_1px,transparent_1px),linear-gradient(90deg,rgba(34,211,238,0.08)_1px,transparent_1px)] [background-size:48px_48px]"/><div className="absolute left-[-10%] top-[18%] h-px w-[120%] bg-gradient-to-r from-transparent via-cyan-300/60 to-transparent shadow-[0_0_32px_rgba(34,211,238,0.9)]"/><div className="absolute left-[12%] top-0 h-full w-px bg-gradient-to-b from-transparent via-cyan-300/25 to-transparent"/></div>}
function ClickSparkLayer({sparks}){return <div className="pointer-events-none fixed inset-0 z-50">{sparks.map(s=><span key={s.id} className="absolute block h-16 w-16 -translate-x-1/2 -translate-y-1/2 rounded-full border border-cyan-200/90 bg-cyan-300/20 shadow-[0_0_42px_rgba(34,211,238,1)] animate-ping" style={{left:s.x,top:s.y}} />)}</div>}

function AnalysisReport({item}){if(!item)return null;return <div className="mt-5 rounded-[2rem] border border-cyan-300/25 bg-slate-950/80 p-6 shadow-[0_0_34px_rgba(34,211,238,0.18),inset_0_0_24px_rgba(34,211,238,0.06)]"><div className="flex flex-col gap-2 border-b border-cyan-300/15 pb-4 md:flex-row md:items-end md:justify-between"><div><div className="text-xs font-black tracking-[0.28em] text-cyan-300/80">AI STRATEGY REPORT</div><div className="mt-2 text-3xl font-black tracking-[0.08em] text-cyan-50 drop-shadow-[0_0_18px_rgba(34,211,238,0.75)]">AI分析建議</div></div><div className="rounded-2xl border border-cyan-300/20 bg-cyan-400/10 px-4 py-2 text-right"><div className="text-xs text-cyan-200/70">操作判定</div><div className="text-2xl font-black text-white">{item.actionLevel}</div></div></div><div className="mt-5 rounded-3xl border border-cyan-300/20 bg-cyan-500/10 p-5"><div className="text-sm font-black tracking-[0.18em] text-cyan-200">白話結論</div><p className="mt-3 text-2xl font-black leading-relaxed text-white md:text-3xl">{item.actionText}</p></div><div className="mt-5 grid gap-4 md:grid-cols-2"><div className="rounded-3xl border border-white/10 bg-white/[0.04] p-5"><div className="text-sm font-black tracking-[0.16em] text-cyan-200">盤勢解讀</div><p className="mt-3 text-base leading-8 text-slate-200 md:text-lg">{item.advice}</p></div><div className="rounded-3xl border border-white/10 bg-white/[0.04] p-5"><div className="text-sm font-black tracking-[0.16em] text-cyan-200">本金適配</div><p className="mt-3 text-base leading-8 text-slate-200 md:text-lg">{item.bankrollAdvice}</p></div></div><div className="mt-5 grid gap-4 md:grid-cols-3"><MetricCard title="綜合機會" value={item.posteriorOpportunity} tone="emerald"/><MetricCard title="主要風險" value={Math.max(item.chaseRisk||0,item.pullbackRisk||0)} tone="rose"/><MetricCard title="操作分數" value={item.operationScore} tone="yellow"/></div><div className="mt-5 grid gap-3 text-sm text-slate-300 md:grid-cols-2"><div className="rounded-2xl bg-white/[0.04] p-4">房號：<b className="text-cyan-100">{item.roomNo}</b></div><div className="rounded-2xl bg-white/[0.04] p-4">曲線區間：<b className="text-cyan-100">{item.phase}</b></div><div className="rounded-2xl bg-white/[0.04] p-4">前兩次記憶：<b className="text-cyan-100">{item.recentOpenMemory}</b></div><div className="rounded-2xl bg-white/[0.04] p-4">低轉反差：<b className="text-cyan-100">{item.earlyTriggerContrast}</b></div><div className="rounded-2xl bg-white/[0.04] p-4">高轉壓力：<b className="text-cyan-100">{item.highTurnPressure}</b></div><div className="rounded-2xl bg-white/[0.04] p-4">可信度：<b className="text-cyan-100">{item.confidence}</b></div></div></div>}

export default function App(){
  const [passcode,setPasscode]=useState(''),[showPass,setShowPass]=useState(false),[role,setRole]=useState(null),[loginError,setLoginError]=useState(''),[minutes,setMinutes]=useState(30),[form,setForm]=useState(DEFAULT_FORM),[masterHistory,setMasterHistory]=useState([]),[tempHistoryMap,setTempHistoryMap]=useState({}),[activeTempCode,setActiveTempCode]=useState(null),[sparks,setSparks]=useState([]),[selectedHistory,setSelectedHistory]=useState(null),[toast,setToast]=useState(null);
  const [tempCodes,setTempCodes]=useState([{code:'SET-DEMO88',minutes:30,createdAt:Date.now(),expiresAt:Date.now()+30*60*1000,active:true}]);
  const isMaster=role==='master', currentHistory=isMaster?masterHistory:(tempHistoryMap[activeTempCode]||[]), latest=currentHistory[0];
  const setFormValue=(k,v)=>setForm(o=>({...o,[k]:v}));
  const showToast=m=>{const id=safeId();setToast({id,message:m});setTimeout(()=>setToast(o=>o?.id===id?null:o),2200)};
  const tempStatus=i=>!i.active?'已停用':Date.now()>i.expiresAt?'已過期':'可使用';
  const handleLogin=()=>{setLoginError(''); if(passcode===MASTER_CODE){setRole('master');setActiveTempCode(null);return;} const c=tempCodes.find(i=>i.code===passcode); if(c&&c.active&&Date.now()<=c.expiresAt){setRole('temp');setActiveTempCode(c.code);return;} setLoginError('通行碼錯誤、已停用或已過期')};
  const calculate=()=>{const rn=Number(form.roomNo); if(!form.roomNo||!Number.isInteger(rn)||rn<1||rn>3500){showToast('房號請輸入1-3500區間');return;} const result=analyzeMachine(form,isMaster); if(isMaster)setMasterHistory(o=>[result,...o]); else if(activeTempCode)setTempHistoryMap(o=>({...o,[activeTempCode]:[result,...(o[activeTempCode]||[])]}));};
  const createCode=()=>{const safe=Math.max(1,Number(minutes||1));setTempCodes(o=>[{code:makeTempCode(),minutes:safe,createdAt:Date.now(),expiresAt:Date.now()+safe*60000,active:true},...o])};
  const extendCode=(code,add)=>setTempCodes(o=>o.map(i=>i.code===code?{...i,expiresAt:Math.max(Date.now(),i.expiresAt)+add*60000,active:true}:i));
  const toggleCode=code=>setTempCodes(o=>o.map(i=>i.code===code?{...i,active:!i.active}:i));
  const logout=()=>{setRole(null);setPasscode('');setActiveTempCode(null);setForm(DEFAULT_FORM)};
  const clickFx=e=>{const id=safeId();setSparks(o=>[...o.slice(-5),{id,x:e.clientX,y:e.clientY}]);setTimeout(()=>setSparks(o=>o.filter(s=>s.id!==id)),650)};

  if(!role) return <div onPointerDown={clickFx} className="relative min-h-screen overflow-hidden bg-[radial-gradient(circle_at_top,#155e75_0%,#020617_42%,#000_100%)] px-5 py-8 text-white"><CircuitBackground/><ClickSparkLayer sparks={sparks}/><div className="relative z-10 mx-auto flex min-h-[88vh] max-w-md items-center justify-center"><motion.div initial={{opacity:0,y:18}} animate={{opacity:1,y:0}} className="w-full"><Card className={PANEL}><CardContent className="relative p-8"><div className="mb-8 text-center"><div className="mx-auto mb-5 flex h-16 w-16 items-center justify-center rounded-3xl border border-cyan-300/40 bg-cyan-400/15 shadow-[0_0_30px_rgba(34,211,238,0.9)]"><Cpu className="h-8 w-8 text-cyan-200"/></div><h1 className="relative inline-block text-3xl font-black tracking-[0.18em] text-cyan-100 drop-shadow-[0_0_22px_rgba(34,211,238,0.95)] md:text-5xl">賽特AI選房<br/>輔助系統</h1><p className="mt-3 text-sm text-cyan-100/70">SET AI ROOM ASSISTANT</p></div><div className="space-y-4"><label className="block"><div className="mb-2 flex items-center gap-2 text-xs tracking-widest text-cyan-200/80"><Lock className="h-4 w-4"/>輸入通行碼</div><div className="relative"><input type={showPass?'text':'password'} value={passcode} onChange={e=>setPasscode(e.target.value)} onKeyDown={e=>e.key==='Enter'&&handleLogin()} placeholder="請輸入通行碼" className="w-full rounded-2xl border border-cyan-300/20 bg-slate-950/70 px-4 py-4 pr-14 text-center text-lg tracking-widest text-white outline-none transition focus:border-cyan-300/70 focus:ring-2 focus:ring-cyan-400/20"/><button type="button" onClick={()=>setShowPass(o=>!o)} className="absolute right-4 top-1/2 -translate-y-1/2 rounded-xl p-2 text-cyan-100/70 hover:bg-cyan-400/10">{showPass?<EyeOff className="h-5 w-5"/>:<Eye className="h-5 w-5"/>}</button></div></label>{loginError&&<div className="rounded-2xl border border-rose-400/30 bg-rose-500/10 px-4 py-3 text-sm text-rose-100">{loginError}</div>}<Button onClick={handleLogin} className="w-full rounded-2xl bg-cyan-300 py-6 text-base font-black text-slate-950 shadow-[0_0_24px_rgba(34,211,238,0.8)] hover:bg-cyan-200">登入</Button></div></CardContent></Card></motion.div></div></div>;

  return <div onPointerDown={clickFx} className="relative min-h-screen overflow-hidden bg-[radial-gradient(circle_at_top_left,#0e7490_0%,#020617_34%,#000_100%)] px-4 py-5 text-white sm:px-6 lg:px-8"><CircuitBackground/><ClickSparkLayer sparks={sparks}/>{toast&&<div className="pointer-events-none fixed left-1/2 top-6 z-[90] -translate-x-1/2 rounded-2xl border border-rose-300/30 bg-rose-500/20 px-6 py-4 text-sm font-black text-rose-50 shadow-[0_0_28px_rgba(244,63,94,0.45)] backdrop-blur-xl">{toast.message}</div>}<div className="relative z-10 mx-auto max-w-7xl"><header className="relative mb-6 flex flex-col gap-4 overflow-hidden rounded-[2rem] border border-cyan-300/40 bg-slate-950/75 p-5 shadow-[0_0_30px_rgba(34,211,238,0.22)] backdrop-blur-xl md:flex-row md:items-center md:justify-between"><div><div className="mb-2 flex items-center gap-2 text-sm text-cyan-100/80"><Sparkles className="h-4 w-4"/>AI ANALYSIS CONSOLE</div><h1 className="relative inline-block text-3xl font-black tracking-[0.18em] text-cyan-100 drop-shadow-[0_0_22px_rgba(34,211,238,0.95)] md:text-5xl">賽特AI選房輔助系統</h1><p className="mt-2 text-sm text-slate-300">目前身份：{isMaster?'最高權限管理者':`臨時通行使用者 ${activeTempCode||''}`}</p></div><Button onClick={logout} className="rounded-2xl bg-white/10 px-5 py-5 text-white hover:bg-white/20"><Power className="mr-2 h-4 w-4"/>登出</Button></header><div className="space-y-6"><Card className={PANEL}><CardContent className="p-5 md:p-7"><div className="mb-6 flex items-center gap-3"><div className="rounded-2xl bg-cyan-400/10 p-3 text-cyan-200"><Search className="h-6 w-6"/></div><div><Title>數據輸入</Title><p className="mt-4 text-sm text-slate-400">輸入機台目前數據後開始分析</p></div></div><div className="grid gap-4 md:grid-cols-5"><Field label="房號" value={form.roomNo} setValue={v=>setFormValue('roomNo',v)} placeholder="例如 168" integerOnly/><Field label="本金" value={form.bankroll} setValue={v=>setFormValue('bankroll',v)} placeholder="例如 4196.5"/><Field label="未開轉數" value={form.unopened} setValue={v=>setFormValue('unopened',v)} placeholder="例如 68" integerOnly/><Field label="前一" value={form.prev1} setValue={v=>setFormValue('prev1',v)} placeholder="例如 42" integerOnly/><Field label="前二" value={form.prev2} setValue={v=>setFormValue('prev2',v)} placeholder="例如 31" integerOnly/></div><div className="mt-4 grid gap-4 md:grid-cols-2"><Field label="當日投注量" value={form.todayBet} setValue={v=>setFormValue('todayBet',v)} placeholder="例如 563200.75"/><Field label="當日得分率" value={form.todayRate} setValue={v=>setFormValue('todayRate',v)} placeholder="例如 76.4"/></div><div className="mt-4 grid gap-4 md:grid-cols-2"><Field label="近30日投注量" value={form.monthBet} setValue={v=>setFormValue('monthBet',v)} placeholder="例如 12800000.9"/><Field label="近30日得分率" value={form.monthRate} setValue={v=>setFormValue('monthRate',v)} placeholder="例如 90.2"/></div><Button onClick={calculate} className="mt-6 w-full rounded-2xl bg-cyan-300 py-6 text-base font-black text-slate-950 hover:bg-cyan-200">啟動AI分析</Button></CardContent></Card><Card className={PANEL}><CardContent className="p-5 md:p-7"><div className="mb-5 flex items-center gap-3"><div className="rounded-2xl bg-cyan-400/10 p-3 text-cyan-200"><Cpu className="h-6 w-6"/></div><div><Title>當前分析結果</Title><p className="mt-4 text-sm text-slate-400">AI資金曲線分析</p></div></div>{latest?<><div className="grid gap-4 lg:grid-cols-4"><MetricCard title="目前狀態" value={latest.label}/><MetricCard title="回分傾向" value={latest.returnScore} tone="emerald"/><MetricCard title="咬分壓力" value={latest.biteScore} tone="rose"/><MetricCard title="本金適配" value={latest.bankrollFit} tone="yellow"/></div><div className="mt-5 grid gap-4 md:grid-cols-4"><MetricCard title="資金推進率" value={`${latest.volumeRatio}%`}/><MetricCard title="得分率落差" value={latest.rateGap}/><MetricCard title="波動壓力" value={latest.riskIndex} tone="rose"/><MetricCard title="可信度" value={latest.confidence} tone="emerald"/></div><AnalysisReport item={latest}/></>:<div className="rounded-3xl border border-white/10 bg-white/[0.04] p-6 text-slate-400">尚未產生分析結果</div>}</CardContent></Card><Card className={PANEL}><CardContent className="p-5 md:p-7"><div className="mb-5 flex items-center gap-3"><div className="rounded-2xl bg-cyan-400/10 p-3 text-cyan-200"><History className="h-6 w-6"/></div><Title>歷史查詢紀錄</Title></div><div className="space-y-3">{currentHistory.length===0&&<div className="rounded-3xl border border-white/10 bg-white/[0.04] p-5 text-sm text-slate-400">尚無查詢紀錄</div>}{currentHistory.map(item=><button key={item.id} onClick={()=>setSelectedHistory(item)} className="w-full rounded-3xl border border-white/10 bg-white/[0.04] p-4 text-left hover:border-cyan-300/40 hover:bg-cyan-400/10"><div className="flex items-start justify-between gap-3"><div><div className="font-black text-cyan-100">房號 {item.roomNo}｜{item.label}</div><div className="mt-1 text-xs text-slate-400">{item.time}</div><div className="mt-2 text-xs text-slate-500">資金推進率 {item.volumeRatio}%｜本金適配 {item.bankrollFit}</div></div><div className="text-right text-sm"><div className="text-emerald-200">回 {item.returnScore}</div><div className="text-rose-200">咬 {item.biteScore}</div></div></div></button>)}</div></CardContent></Card>{isMaster&&<Card className={PANEL}><CardContent className="p-5 md:p-7"><div className="mb-5 flex items-center gap-3"><div className="rounded-2xl bg-cyan-400/10 p-3 text-cyan-200"><KeyRound className="h-6 w-6"/></div><div><Title>臨時通行碼管理</Title><p className="mt-4 text-sm text-slate-400">生成、停用、延長使用時間</p></div></div><div className="flex gap-3"><input type="number" value={minutes} onChange={e=>setMinutes(e.target.value)} className="w-full rounded-2xl border border-cyan-300/20 bg-slate-950/70 px-4 py-3 text-white outline-none"/><Button onClick={createCode} className="rounded-2xl bg-cyan-300 px-5 text-slate-950 hover:bg-cyan-200"><Plus className="h-4 w-4"/></Button></div><div className="mt-5 space-y-3">{tempCodes.map(i=><div key={i.code} className="rounded-3xl border border-white/10 bg-white/[0.04] p-4"><div className="flex items-center justify-between gap-3"><div><div className="font-black tracking-widest text-cyan-100">{i.code}</div><div className="mt-1 text-xs text-slate-400">{tempStatus(i)}｜到期 {new Date(i.expiresAt).toLocaleTimeString('zh-TW',{hour12:false})}</div></div><div className="flex gap-2"><Button onClick={()=>extendCode(i.code,10)} className="rounded-xl bg-white/10 px-3 py-2 text-xs text-white hover:bg-white/20">+10分</Button><Button onClick={()=>toggleCode(i.code)} className="rounded-xl bg-white/10 px-3 py-2 text-xs text-white hover:bg-white/20">{i.active?'停用':'啟用'}</Button></div></div></div>)}</div></CardContent></Card>}</div><div className="mt-6 rounded-[2rem] border border-cyan-300/20 bg-slate-950/50 p-4 text-center text-xs text-slate-400 backdrop-blur-xl">本系統為虛擬輔助介面與娛樂性數據評估，不保證任何實際結果與收益</div>{selectedHistory&&<div className="fixed inset-0 z-[80] flex items-center justify-center bg-black/75 p-4 backdrop-blur-md" onClick={()=>setSelectedHistory(null)}><div className="max-h-[88vh] w-full max-w-5xl overflow-y-auto rounded-[2rem] border border-cyan-300/30 bg-slate-950 p-5 shadow-[0_0_60px_rgba(34,211,238,0.35)]" onClick={e=>e.stopPropagation()}><div className="mb-5 flex flex-col gap-4 border-b border-cyan-300/15 pb-5 md:flex-row md:items-center md:justify-between"><div><div className="text-xs font-black tracking-[0.28em] text-cyan-300/80">HISTORY DETAIL</div><div className="mt-2 text-2xl font-black text-cyan-50">房號 {selectedHistory.roomNo}｜詳細AI分析</div></div><Button onClick={()=>setSelectedHistory(null)} className="rounded-2xl bg-white/10 px-5 py-5 text-white hover:bg-white/20">關閉</Button></div><div className="grid gap-4 lg:grid-cols-4"><MetricCard title="目前狀態" value={selectedHistory.label}/><MetricCard title="回分傾向" value={selectedHistory.returnScore} tone="emerald"/><MetricCard title="咬分壓力" value={selectedHistory.biteScore} tone="rose"/><MetricCard title="本金適配" value={selectedHistory.bankrollFit} tone="yellow"/></div><AnalysisReport item={selectedHistory}/></div></div>}</div></div>;
}
